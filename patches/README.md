Apply against manager.html on main / feature branch to enable Wine/Retail Square-only mode in Cheese Case.

```
patch -p0 < patches/wine-retail-manager.html.patch
```

Or open PR after applying locally; Render deploys manager.html on merge.
