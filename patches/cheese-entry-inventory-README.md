# Cheese Entry inventory + Wine/Retail Square-only (manager.html)

```bash
patch -p1 < patches/cheese-entry-inventory.patch
```

Adds Track inventory (default ON retail / OFF cheese), Memphis/Nashville starting qty, hides DateCode-only Wine/Retail UI.
